import React from "react";
import App from "/.App.js";
import { ReactDOM } from "react";

RractDom.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <App/>
    </React.StrictMode>
);